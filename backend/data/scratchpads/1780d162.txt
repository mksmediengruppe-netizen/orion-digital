Файл 27448 символов - слишком большой для file_write (лимит ~8000). Нужно использовать Python на сервере с base64. Стратегия: передать base64 по частям через несколько SSH команд, записывая в файл /tmp/b64.txt, потом декодировать одной командой.